name = "api-contract-verifier"
description = "Evaluates the contract_verified gate by diffing the Spring Boot OpenAPI shape against the target API specification. Reports only, never fixes."
model = "gpt-5-codex"
model_reasoning_effort = "medium"

developer_instructions = """
Load and follow `.agents/roles/api-contract-verifier.md`.

Read and follow:
- AGENTS.md
- .agents/rules/llm-behavior.md
- .agents/rules/scm-conventions.md
- .agents/rules/command-execution.md
- .agents/rules/project-conventions.md for feature, bug, chore, and docs work
- .agents/rules/migration-conventions.md for migration work

Use the repository tools available in the current Codex session. If a referenced custom
agent wrapper or skill is unavailable, proceed inline from the shared role instructions.
Role id: api-contract-verifier
"""
